streamlit>=1.60,<2.0
opencv-python-headless>=4.9,<5.0
numpy>=1.26,<3.0
Pillow>=10.0,<12.0
pandas>=2.0,<3.1
PyMuPDF>=1.24,<2.0
pytesseract>=0.3.10,<0.4
openpyxl>=3.1,<4.0
nepali_datetime>=1.0.8,<2.0

# Optional: enables "draw directly on the page" ROI editing. The app works
# fine without it (falls back to numeric X/Y/Width/Height entry) -- install
# this only if you want the drawing canvas and are prepared for it to be the
# least actively-maintained dependency in this list.
streamlit-drawable-canvas>=0.9.3,<1.0

pytest>=8.0,<9.0
